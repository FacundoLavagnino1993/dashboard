table#filters(class="table")
    thead
        th Filtrar Id
        th Filtrar Estado
        th Flltrar Etapa
        th Filtrar Disponibilidad
        th Filtrar Fecha
    tbody.filterHeader
        td
            filter-by-id(filters="$ctrl.filters" works="$ctrl.works" tasks-filtered='$ctrl.tasksFiltered')
        td
            filter-by-state(works="$ctrl.works" tasks-filtered='$ctrl.tasksFiltered')
        td
            filter-by-stage(works="$ctrl.works" tasks-filtered='$ctrl.tasksFiltered')
        td
            filter-by-availability(works="$ctrl.works" tasks-filtered='$ctrl.tasksFiltered')
        td
            filter-by-date(works="$ctrl.works" tasks-filtered='$ctrl.tasksFiltered')
